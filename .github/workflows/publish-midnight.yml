name: Publish Midnight to PyPI

on:
  push:
    tags:
      - "midnight-v*"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - run: python -m pip install --upgrade build twine
      - name: Build all-night-midnight distribution
        run: python -m build --wheel --outdir dist packages/midnight
      - run: twine check dist/*
      - uses: actions/upload-artifact@v4
        with:
          name: midnight-python-package-distributions
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/all-night-midnight
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: midnight-python-package-distributions
          path: dist/
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
