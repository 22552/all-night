[project]
name = "night-cloudflare-worker-example"
version = "0.1.0"
description = "Night framework example for Cloudflare Python Workers"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
