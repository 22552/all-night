# Local application configuration. Copy to .env; do not commit .env.
NIGHT_SECRET_KEY=replace-with-a-long-random-value

# PyPI publishing does not read this file. Set PYPI_API_TOKEN as a GitHub
# Actions repository secret instead.

