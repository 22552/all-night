[project]
name = "night-cloudflare-deploy"
version = "0.1.0"
description = "One-click Cloudflare Python Worker demo for the Night framework"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
